[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.broker"

[deploy]
startCommand = "uvicorn apps.api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 1000
restartPolicyType = "ON_FAILURE"

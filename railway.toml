[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/Dockerfile.broker"

[deploy]
startCommand = "uvicorn apps.broker.broker:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 1000
restartPolicyType = "ON_FAILURE"
